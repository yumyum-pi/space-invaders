#include <assert.h>
#include <stdbool.h>
// need to simulate a ball falling on the ground
int main() {
  // steps to folllow
  // create a rendere
  // create the ball
  // update the ball's position

  return 0;
}
